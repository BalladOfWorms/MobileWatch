# Copy this file to keystore.properties (same folder) and fill it in.
# keystore.properties is gitignored; this example file is not.
# storeFile is resolved relative to the project root.
storeFile=mobilewatch-release.jks
storePassword=CHANGEME
keyAlias=mobilewatch
keyPassword=CHANGEME
